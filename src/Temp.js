import React from "react";
import { Button } from "react-bootstrap";

const Temp = () => {
  return (
    <div>
      <Button varient="primary">button</Button>
    </div>
  );
};

export default Temp;
